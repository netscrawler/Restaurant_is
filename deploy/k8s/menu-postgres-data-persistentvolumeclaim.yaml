apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: menu-postgres-data
  name: menu-postgres-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
