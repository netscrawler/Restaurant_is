apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: order-postgres-data-replica
  name: order-postgres-data-replica
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
