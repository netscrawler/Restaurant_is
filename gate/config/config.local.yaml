server:
  host: "0.0.0.0"
  port: 8080

auth:
  jwt_secret: "your-secret-key"

services:
  auth:
    host: "localhost"
    port: 50051
  user:
    host: "localhost"
    port: 50052
  menu:
    host: "localhost"
    port: 50053
  order:
    host: "localhost"
    port: 50054
  notify:
    host: "localhost"
    port: 50055
