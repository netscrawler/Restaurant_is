apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: menu-postgres-data-replica
  name: menu-postgres-data-replica
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
