apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: auth-postgres-data-replica
  name: auth-postgres-data-replica
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
